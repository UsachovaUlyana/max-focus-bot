/**
 * Шаблоны сообщений бота
 */

export const messages = {
  start: `⚡ *MAX Focus Pods*

Твоя экосистема продуктивности в MAX

⏱️ Pomodoro 25/50/90 — без отвлечений
🤝 Focus Pods — генерируй код и собирай команду
📋 Smart задачи — дедлайны, распил на подзадачи
🪙 FocusCoins — streak, достижения и мотивация

Выбирай действие ниже или напиши команду вручную`,

  help: `❓ *Помощь*

*Доступные команды:*

\`/start\` — Главное меню
\`/tasks\` — Мои задачи
\`/focus\` — Запустить Pomodoro 25 минут
\`/pod\` — Создать фокус-Pod
\`/join <код>\` — Присоединиться к Pod
\`/profile\` — Мой профиль
\`/help\` — Показать эту справку

───────────────────

*Основные функции:*

⏱️ *Фокус-сессии*
Pomodoro таймер на 25/50/90 минут
После завершения получаешь FocusCoins
Можно отменить или завершить досрочно

🎯 *Focus Pods*
Создавай совместные фокус-сессии
Приглашай друзей кодом: \`/join A4F2B9E1\`
Бонусные награды за групповую работу

📋 *Задачи*
Добавляй задачи: _"Сдать отчёт до 20.11 12:00"_
Распиляй на подзадачи, откладывай, завершай
Автоматический парсинг дедлайнов

🪙 *FocusCoins*
Виртуальная валюта за продуктивность
Зарабатывай за задачи и сессии

🏆 *Достижения*
Разблокируй за активность и получай награды

🔥 *Серия дней*
Используй бота каждый день → больше наград`,

  addTaskPrompt: `📝 *Добавить задачу*

Просто напиши мне задачу — я пойму!

Примеры:
• Подготовить презентацию до завтра 15:00
• Позвонить клиенту через 2 часа
• Купить продукты сегодня

Я автоматически распознаю дедлайны 📅`,

  taskAdded: (title: string, deadline?: string) => {
    let msg = `✅ *Задача добавлена!*\n\n📌 ${title}`;
    if (deadline) {
      msg += `\n📅 Дедлайн: ${deadline}`;
    }
    return msg;
  },

  noTasks: `У тебя пока нет задач.\n\nДобавь новую через кнопку "➕ Задача" 📝`,

  focusStart: (duration: number) => `⏱️ Запускаем ${duration} минут фокуса!

Выключи отвлечения и сосредоточься на задаче.

Напоминание придёт, когда время выйдет! 💪`,

  focusComplete: (reward: number, streak: number) => `🎉 Отлично! Фокус-сессия завершена!

+${reward} FocusCoins 🪙
Серия: ${streak} дней 🔥

Что ты сделал за это время?`,

  sessionCancelled: `❌ Сессия отменена.

Ничего страшного! Попробуй снова, когда будешь готов 💪`,

  podCreated: (title: string, duration: number, link: string) => `🎯 Pod создан!

*${title}*
⏱️ Длительность: ${duration} минут

📤 Пригласи друзей:
${link}

Когда все будут готовы, нажми "Начать"!`,

  podJoined: (title: string, participants: number) => `✅ Присоединился к Pod'у!

*${title}*
👥 Участников: ${participants}

Ожидаем начала сессии...`,

  podStarted: (title: string, duration: number) => `🚀 Pod "${title}" начался!

⏱️ ${duration} минут фокуса
👥 Все участники работают вместе

Выключи отвлечения! 💪`,

  podCompleted: `🎉 Pod завершён!

Что ты успел сделать?`,

  achievementUnlocked: (name: string, description: string, icon: string, reward: number) => 
    `🎊 *НОВОЕ ДОСТИЖЕНИЕ!*\n\n` +
    `${icon} *${name}*\n` +
    `${description}\n\n` +
    `💰 +${reward} FocusCoins\n\n` +
    `Ты на верном пути! Продолжай в том же духе! 🚀`,

  dailyReminder: (pomodoros: number) => `Привет! 👋

У тебя ${pomodoros} Pomodoro сегодня. Может, начнёшь? 🚀`,

  streakWarning: (streak: number, hoursLeft: number) => `⚠️ Твоя серия в опасности!

${streak} дней 🔥

Осталось ${hoursLeft} часов до конца дня. Сделай хотя бы один Pomodoro!`,

  weeklyStats: (stats: any) => `📊 Твоя статистика за неделю:

🎯 Pomodoro: ${stats.pomodoros}
⏱️ Фокуса: ${Math.floor(stats.focusMinutes / 60)}ч ${stats.focusMinutes % 60}мин
✅ Задач выполнено: ${stats.tasksCompleted}
🪙 FocusCoins: +${stats.focusCoins}

Отличная работа! Так держать! 💪`,

  profile: (user: any, stats: any) => {
    let msg = `👤 Твой профиль\n\n`;
    msg += `🪙 FocusCoins: ${user.focusCoins}\n`;
    msg += `🔥 Серия: ${user.currentStreak} дней (лучшая: ${user.bestStreak})\n\n`;
    
    msg += `📊 Статистика:\n`;
    msg += `• Pomodoros: ${user.totalPomodoros}\n`;
    msg += `• Фокуса: ${Math.floor(user.totalFocusMinutes / 60)}ч ${user.totalFocusMinutes % 60}мин\n`;
    msg += `• Задач: ${user.completedTasks}\n\n`;
    
    if (stats.weekStats) {
      msg += `На этой неделе:\n`;
      msg += `• Pomodoros: ${stats.weekStats.pomodoros}\n`;
      msg += `• Фокуса: ${Math.floor(stats.weekStats.focusMinutes / 60)}ч ${stats.weekStats.focusMinutes % 60}мин\n`;
      msg += `• Задач: ${stats.weekStats.tasksCompleted}\n\n`;
    }
    
    msg += `🏆 Достижения: ${stats.achievements.unlocked}/${stats.achievements.total} (${stats.achievements.progress}%)`;
    
    return msg;
  },

  achievements: (achievements: any[]) => {
    let msg = `🏆 Достижения\n\n`;
    
    const unlocked = achievements.filter(a => a.unlocked);
    const locked = achievements.filter(a => !a.unlocked);
    
    if (unlocked.length > 0) {
      msg += `✅ Разблокировано:\n`;
      unlocked.forEach(a => {
        msg += `${a.icon} ${a.name} (+${a.reward} 🪙)\n`;
        msg += `   ${a.description}\n\n`;
      });
    }
    
    if (locked.length > 0) {
      msg += `🔒 Ещё не разблокировано:\n`;
      locked.forEach(a => {
        msg += `${a.icon} ${a.name} (${a.progress}%)\n`;
        msg += `   ${a.description}\n`;
        msg += `   Награда: ${a.reward} 🪙\n\n`;
      });
    }
    
    return msg;
  },

  taskNotFound: `❌ Задача не найдена.`,

  taskCompleted: (title: string, reward: number) => `✅ Задача выполнена!

${title}

+${reward} FocusCoins 🪙`,

  taskSplit: (originalTitle: string, subtasksCount: number) => `✂️ Задача распилена!

Оригинальная: ${originalTitle}

Создано подзадач: ${subtasksCount}

Теперь их легче выполнить! 💪`,

  taskPostponed: (title: string, newDeadline: string) => `⏸️ Задача отложена

${title}

Новый дедлайн: ${newDeadline}`,

  taskDeleted: `🗑️ Задача удалена`,

  error: `❌ Произошла ошибка. Попробуй ещё раз.`,

  unknownCommand: `🤔 Не понял команду.

Используй меню или команду /start`,

  podNotFound: `❌ Pod не найден.`,

  podAlreadyStarted: `⚠️ Pod уже начался.`,

  podCancelled: `❌ Pod отменён.`,

  joinQRPrompt: `📱 Готов сканировать QR?

Отправь мне фото QR-кода или ссылку на Pod.`
};

